{
  "name": "@tailsec/scan-secrets",
  "version": "0.1.0",
  "description": "Secret scanning tool — find API keys, tokens, passwords in code and git history",
  "license": "MIT",
  "type": "module",
  "bin": {
    "tailsec-scan-secrets": "./dist/cli.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/tailsec-com/scan-secrets"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "prepublish": "tsc",
    "test": "node --experimental-vm-modules --max-old-space-size=4096 node_modules/jest/bin/jest.js",
    "sync-patterns": "node scripts/sync-patterns.ts"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "commander": "^11.1.0",
    "glob": "^10.3.10"
  },
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "@types/node": "^20.10.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.9",
    "typescript": "^5.3.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
